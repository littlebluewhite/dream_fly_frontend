<script lang="ts">
  /* /staff/login — placeholder; replaced by the staff-login implementation.
   * `+page@.svelte` resets to the root layout (full-screen, no coach/marketing
   * chrome); the root layout's /staff bypass removes the marketing header. */
</script>

<div>員工後台登入</div>
